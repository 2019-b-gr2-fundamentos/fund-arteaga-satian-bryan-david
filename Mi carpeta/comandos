
tranquilar de ts a javascript javascript :  tsc lib ES5 "Nombre del archivo"
//.length sirva para contar el número de caracteres.

para compilar en c++:
g++ nombrearchivo.cpp -o nombrearchivo.exe
/* con eso creo que hace un build 
luego,
solo escribir nombrearchivo
y ya se compila.

Para hacer un console.log con variabeles y texto >
console.log(`blablabla ${} asffdas);
    

    Para debugear es necesario usar el copy trelative path y siempre crear un debugger para cada 
    programa


    el npm publish se lo usa en la libreria
    para instalar nuevas librerias se instala el npm install

    david-libreriauno": "^1.1.0" el ^ sirve para que se instale la version mas actual
    npm install "nombre de la libreria" y se installa y ademas se actualiza la version de package.json


    para crear el package.jso es el npm init
    luego crear un .gitignore para no tomar en cuenta los node_modules que se crean cuando se instala una libreria
    para instalar u a libreria es npm install

    "start": "(tsc 02-crud.ts --target ES5|| echo) && node 02-crud.js" --> con esto podemos tranquilar el archivo sin necesidad de 
    hacer en la node comand
    

    
